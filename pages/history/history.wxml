<view class="content">
  <view class='container'>
    <block wx:for="{{logs}}" wx:for-item="log">
      <view class="item">{{log}}</view>
    </block>
  </view>
  <view class='clear' wx:if="{{logs.length>0}}" bindtap='clear'>清除历史记录</view>
</view>